const fs = require('fs'); const content = fs.readFileSync('c:/Users/user/source/repos/RentGuard-360/frontend/src/contexts/LanguageContext.jsx', 'utf8'); const transStart = content.indexOf('const translations ='); let depth = 0; let transEnd = -1; let started = false; for (let i = transStart; i < content.length; i++) { if (content[i] === '{') { depth++; started = true; } else if (content[i] === '}') { depth--; } if (started && depth === 0) { transEnd = i + 1; break; } } fs.writeFileSync('c:/Users/user/source/repos/RentGuard-360/frontend/temp_translations.txt', content.substring(transStart, transEnd)); fs.writeFileSync('c:/Users/user/source/repos/RentGuard-360/frontend/src/contexts/LanguageContext.jsx', content.substring(0, transStart) + '\\nimport { he } from \\'./locales/he\\';\\nimport { en } from \\'./locales/en\\';\\n\\nconst translations = { he, en };\\n' + content.substring(transEnd).replace(/^;/,''));
